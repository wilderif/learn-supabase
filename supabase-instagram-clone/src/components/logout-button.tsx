'use client';

import { Button } from '@material-tailwind/react';

export default function LogoutButton() {
  return (
    <Button onClick={() => {}} className="bg-red-500 text-white">
      로그아웃
    </Button>
  );
}
